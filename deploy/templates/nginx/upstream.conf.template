# Healer Gateway Manager — Nginx upstream template (placeholder, Phase 1)
#
# Rendered by services/gateway-manager once deployment behavior is
# implemented. Placeholders like {{server_name}} and {{instance_ports}} are
# filled in from Control Plane state (application, release, healthy
# instances) — never edited by hand on the Nginx host.
#
# upstream {{application_slug}} {
#     {{#each healthy_instance_ports}}
#     server 127.0.0.1:{{this}};
#     {{/each}}
# }
#
# server {
#     listen 443 ssl;
#     server_name {{server_name}};
#
#     ssl_certificate     {{cert_path}};
#     ssl_certificate_key {{key_path}};
#
#     location / {
#         proxy_pass http://{{application_slug}};
#     }
# }

import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function ApplicationsPage() {
  return (
    <PlaceholderPage
      title="Applications"
      description="Connect an existing application folder or Git repository."
    />
  );
}

import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function ServersPage() {
  return (
    <PlaceholderPage
      title="Servers"
      description="Register Windows and Linux servers for Healer to manage."
    />
  );
}

import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function HealthPage() {
  return (
    <PlaceholderPage
      title="Health"
      description="Instance health, CPU/RAM/disk usage, and recent/live logs."
    />
  );
}

import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function CertificatesPage() {
  return (
    <PlaceholderPage
      title="Certificates"
      description="Manage references to existing HTTPS certificate and key file paths."
    />
  );
}

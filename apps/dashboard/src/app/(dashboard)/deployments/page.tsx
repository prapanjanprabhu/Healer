import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function DeploymentsPage() {
  return (
    <PlaceholderPage
      title="Deployments"
      description="Deploy applications, choose instance counts, and roll back to the previous working release."
    />
  );
}
